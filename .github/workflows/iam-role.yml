name: 'Terraform'

on: push

defaults:
  run:
    shell: bash
    working-directory: modules/iam-role/

jobs:
  lint:
    uses: kolvin/github-actions/.github/workflows/terraform-format.yaml@7520074b9bf608080231c5227f18f0f3e00af046
